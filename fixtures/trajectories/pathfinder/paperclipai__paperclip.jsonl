{"key": "5ceae52b3842ca47110c198a", "label": "pathfinder", "model": "gpt-5-mini-2025-08-07", "system": "You are helping an outside developer -- someone with no\nprior connection to a project -- choose which open issue to attempt first.\n\nYou are given issues that were open at a fixed point in time, and evidence about\nhow the project treated outside contributions before that point.\n\nRank the issues by one thing only: **how likely is it that an outsider, starting\nfrom nothing, lands a merged pull request resolving this issue?**\n\nThat is not the same as \"which issue is most important\", and it is not the same\nas \"which issue is easiest\". Weigh:\n\n  * whether the issue states a concrete, bounded outcome rather than a wish\n  * whether someone could act on it without private context or a design decision\n    only a maintainer can make\n  * whether the report contains enough to reproduce or locate the problem\n  * whether the project's history suggests work of this shape gets merged\n\nAn issue labelled for beginners is not automatically a good entry point; many\nare aspirational one-liners nobody has scoped. Judge the text, not the label.\n\nReturn at most five, best first. For each, say in one sentence what the person\nwould actually do, and cite the issue's evidence id.", "prompt": "Repository: paperclipai/paperclip\n\nHow this project treated outsiders before the cutoff:\n  outsider_merged: 1\n  outsider_threads: 200\n  median_first_response_hours: 12.0\n\nIssues open at the cutoff (40 of 279 shown):\n\n--- evidence id: issue:paperclipai/paperclip#7281:opened\n    opened 2026-05-31 by kt-ai-infra; 8 comments; labels: none\n    title: Skills Catalog tab returns 500: server resolves manifest via monorepo path not present in published build\n    ## Summary On a fresh global install of `paperclipai` (latest, `2026.529.0`), the **Skills \u2192 Catalog** tab fails with `Internal server error`. The API endpoint `GET /api/skills/catalog` returns **500** because the server looks for the skills-catalog manifest at a monorepo-relative path that does not exist in the published npm layout. ## Error ``` ERROR: GET /api/skills/catalog 500 \u2014 Skills catalog manifest not found at /Users/<user>/.npm/_npx/<hash>/node_modules/@paperclipai/packages/skills-catalog/generated/catalog.json. Run pnpm --filter @paperclipai/skills-catalog build:manifest. at getCatalogManifest (@paperclipai/server/dist/services/skills-catalog.js:20:15) at getCatalogSkills (@paperc\n--- evidence id: issue:paperclipai/paperclip#6007:opened\n    opened 2026-05-14 by nicorodrigues; 7 comments; labels: none\n    title: Issues lock to dead checkoutRunId after run terminates (activeRun=null + persistent 409)\n    ## Symptom Issues become permanently locked to a dead `checkoutRunId` after their owning heartbeat run terminates. Public API surfaces show `activeRun: null` (because `executionRunId` is cleared on termination) but every `POST /api/issues/:id/checkout` returns `409 Issue checkout conflict`, and every mutation that flows through ownership checks fails the same way. We've hit this three times in one Paperclip company over a short window. Each time the only practical remedy was the `POST /api/issues/:id/admin/force-release` endpoint, which is board-gated and not discoverable. ## Reproduction shape 1. Agent A checks out issue I via run R1. `issues.checkoutRunId = R1`, `issues.executionRunId = R1\n--- evidence id: issue:paperclipai/paperclip#6653:opened\n    opened 2026-05-25 by young-opn; 5 comments; labels: none\n    title: request_confirmation: payload.prompt renders as plain text instead of markdown\n    ## Summary `payload.prompt` in `request_confirmation` issue-thread interactions is rendered as a raw text node instead of through the markdown renderer. Markdown (`**bold**`, `` `code` ``, `[link](url)`, lists) appears as plain text in the UI, breaking confirmation workflows where the proposer relies on the same markdown conventions used in plan documents and comments. This is regression-prone because the sibling field `payload.detailsMarkdown` in the same component **does** route through `MarkdownBody`, so the bug is a one-line omission inside the pending-state branch of `RequestConfirmationCard`. ## Reproduction 1. From any agent or user with permission, create a pending `request_confirmat\n--- evidence id: issue:paperclipai/paperclip#6559:opened\n    opened 2026-05-22 by yasnoyas944-ops; 5 comments; labels: none\n    title: add antigravity CLI\n    add antigravity CLI\n--- evidence id: issue:paperclipai/paperclip#6523:opened\n    opened 2026-05-22 by nathandonaldson; 4 comments; labels: none\n    title: Recovery wake loop \u2014 parent issue stuck in 'blocked' with empty 'blockedByIssueIds', repeated source_scoped_recovery_action wakes\n    ## Summary An issue assigned to an agent entered a recovery-wake loop: it was repeatedly set to `status='blocked'` with `blockedByIssueIds=[]` (an internally inconsistent state \u2014 `blocked` should always name blockers, per the `paperclip` skill doc, line 135). The watchdog detected the inconsistency, woke the agent to \"choose and record a valid disposition\", the agent set `in_progress`, something set it back to `blocked` + empty, the loop continued. **Five wakes fired before the agent broke the cycle by naming a real first-class blocker.** Each wake loaded ~200K cached tokens of context plus fresh work \u2014 five wakes on Opus 4.7 = ~$6 of reference-cost token spend for no useful work. ## Observe\n--- evidence id: issue:paperclipai/paperclip#6344:opened\n    opened 2026-05-19 by melanie531; 4 comments; labels: none\n    title: openclaw-gateway adapter: PROTOCOL_VERSION=3 is hardcoded; OpenClaw 2026.5.18 requires 4\n    ## Summary The `@paperclipai/adapter-openclaw-gateway` adapter (Paperclip 0.3.1) hardcodes `PROTOCOL_VERSION = 3` in two locations and is incompatible with current OpenClaw releases, which require **protocol 4**. The connection dies during the `connect` request immediately after `connect.challenge` is received \u2014 before any of the schema-validation issues already filed (e.g. #5997 `paperclip` envelope, #4081, #617) get a chance to manifest. In other words, anyone trying to wire Paperclip 0.3.1 to a current OpenClaw will hit **this** failure first; they never see the `paperclip` envelope rejection until after they patch the protocol version. ## Environment - **Paperclip**: 0.3.1 (`packages/ada\n--- evidence id: issue:paperclipai/paperclip#6302:opened\n    opened 2026-05-18 by arnavbansal12; 4 comments; labels: none\n    title: SEO audit and keyword research\n    **Parent Issue:** MYA-267 (GTM Strategy & Launch Plan) **Priority:** P0 **Phase:** Phase 1 \u2014 Foundation (Weeks 1-4) ## Objective Conduct comprehensive SEO audit and establish keyword targeting strategy for organic growth. ## Tasks ### Audit - [ ] Audit current search presence for paperclip.ing - [ ] Identify current rankings for target keywords - [ ] Analyze competitor SEO (LangGraph, CrewAI, etc.) - [ ] Technical SEO audit (site speed, mobile, meta tags, structured data) ### Keyword Strategy - [ ] Finalize top 20 target keywords - [ ] Map keywords to content pieces - [ ] Identify quick-win opportunities ### Content Calendar - [ ] Create Q2 content calendar (12 weeks) - [ ] Set publishing ca\n--- evidence id: issue:paperclipai/paperclip#6983:opened\n    opened 2026-05-29 by Lattimius; 3 comments; labels: none\n    title: bug: executionPolicy reviewer handoff and release recovery can strand issues\n    ## Summary Paperclip executionPolicy handling has two related runtime defects that can strand policy-driven issues during review/publish handoff: 1. A reviewer PATCH that explicitly sets `assigneeAgentId` while moving out of review can be silently reinterpreted as `changes_requested`, overriding the caller's explicit assignee and routing back to `returnAssignee`. 2. `POST /api/issues/:id/release` clears `checkoutRunId` / `executionRunId` but leaves execution-policy stage state in place, so subsequent recovery PATCHes can remain blocked by the active-reviewer guard. Observed in a local Paperclip deployment while migrating a production workflow to embedded `executionPolicy`. No local `/opt/pap\n--- evidence id: issue:paperclipai/paperclip#6913:opened\n    opened 2026-05-28 by JulianWeiland; 3 comments; labels: none\n    title: Bug: CPU spikes caused by slow DB queries + aggressive frontend polling (request pile-up)\n    ### Summary CPU spikes on the Node.js app are **not** related to the Python openspace workers. The root cause is slow database queries combined with aggressive frontend polling, which creates a request pile-up that floods the Postgres connection pool and pins CPU. ### Root Cause The frontend polls several endpoints continuously. When a query takes ~177s and new polls fire every few seconds, requests stack up faster than they can be served. At the worst spike (01:15:45), **231 requests hit in a single second \u2014 178 of them `/issues`** \u2014 saturating the Postgres connection pool and pinning CPU. ### Worst Offenders (by hit count and response time) | Endpoint | Slow requests | Avg response time | \n--- evidence id: issue:paperclipai/paperclip#6707:opened\n    opened 2026-05-26 by felipeespitalher; 3 comments; labels: none\n    title: Expose plugin-registered tools as MCP tools for Cursor/Claude Code adapters\n    ## Feature Request Plugin tools registered via `agent.tools.register` and `ctx.tools.register()` are not available as native tools in Cursor and Claude Code adapters. Agents using these adapters cannot call plugin tools directly \u2014 they must use workarounds like `gh api` or `curl`. ### Current behavior - Plugin registers tools (e.g. `github_get_repo_structure`, `github_read_file_content`) - Tools appear in Paperclip's tool registry (`/api/plugins/tools`) - Tools are available via `POST /api/plugins/tools/execute` - **But** Cursor/Claude Code agents do NOT see these tools in their tool list - Managed skills can document the tools, but agents cannot invoke them natively ### Expected behavior Pl\n--- evidence id: issue:paperclipai/paperclip#6606:opened\n    opened 2026-05-24 by Omerbahari; 3 comments; labels: none\n    title: adapter-openclaw-gateway: stable 2026.517.0 sends `paperclip` field rejected by protocol-4 gateway (additionalProperties:false)\n    ## Summary The **stable** `paperclipai@2026.517.0` / `@paperclipai/adapter-openclaw-gateway@2026.517.0` sends a top-level `paperclip` property in agent params that the OpenClaw gateway v2026.5.20+ rejects under protocol 4 (`additionalProperties:false` schema). Every agent dispatch fails with: ``` openclaw_gateway_request_failed: invalid agent params: at root: unexpected property 'paperclip' ``` A local hotpatch was applied to `2026.522.0-canary.5` on this installation, but a fresh `npm install paperclipai` (which pulls stable `2026.517.0`) regresses every agent. --- ## Exact Diff Between Stable and Hotpatch **`packages/adapters/openclaw-gateway/src/server/execute.ts`** (approximate, derived \n--- evidence id: issue:paperclipai/paperclip#6415:opened\n    opened 2026-05-20 by mboaventura; 3 comments; labels: none\n    title: Watchdog re-creates `stale_active_run_evaluation` issues on the same runId after a false-positive resolution\n    **Module**: `@paperclipai/server` (verified in v2026.428.0), file `services/recovery/service.js` ## Summary Once an operator marks an evaluation issue as `done` (or `cancelled`), the partial unique index `issues_active_stale_run_evaluation_uq` (in `@paperclipai/db/schema/issues.js`) no longer matches, so the next tick of `scanSilentActiveRuns()` creates another evaluation issue for the same `runId`. Long-running but legitimately silent workloads (e.g. QA test/coverage runs) generate a duplicate per scan tick. ## Repro 1. Agent run goes silent > 1h 2. Watchdog opens evaluation issue 3. Operator closes as false positive 4. ~Next scan tick creates a new evaluation 5. Repeat We observed 9 duplic\n--- evidence id: issue:paperclipai/paperclip#6347:opened\n    opened 2026-05-19 by sauerdaniel; 3 comments; labels: none\n    title: heartbeat recovery can persist invalid Hermes resume state from display IDs and failed runs\n    ### Summary Paperclip's built-in Hermes adapter can enter a persistent resume failure when saved session state is malformed or when a canonical-looking session ID no longer exists in Hermes. Host-side session defenses from #7516 already protect explicit-resume metadata and preserve known-good task-session state. The remaining defects are in the built-in adapter's parsing/codec behavior and its lack of bounded recovery for a missing canonical session. ### Observed Failure Pattern ```text [hermes] Resuming session: 20260519_102328_ Session not found: 20260519_102328_ Use a session ID from a previous CLI run (hermes sessions list). [hermes] Exit code: 1, timed out: false [hermes] Session: from \n--- evidence id: issue:paperclipai/paperclip#6275:opened\n    opened 2026-05-18 by clawdia-ai-assistant; 3 comments; labels: none\n    title: executionWorkspacePolicy isolated_workspace silently demoted to project_primary at heartbeat checkout (refile of #2589)\n    ## Bug Description `executionWorkspacePolicy.defaultMode: \"isolated_workspace\"` with a `git_worktree` strategy is set on the project, but heartbeat checkout does not provision an execution workspace. Agents run in `project_primary` mode against the project's primary checkout instead of an isolated worktree under `.paperclip/worktrees/`. This is the same failure mode as the previously closed-by-reporter [#2589](https://github.com/paperclipai/paperclip/issues/2589). That reporter closed it as \"not yet verified or fixed. May refile once confirmed.\" We have now reproduced and verified it deterministically across four projects and are refiling with full evidence. ## Environment - Paperclip server\n--- evidence id: issue:paperclipai/paperclip#6272:opened\n    opened 2026-05-18 by remi9martin; 3 comments; labels: none\n    title: Adapter option for per-(agent, issue) workspace isolation across concurrent heartbeats\n    # Feature request \u2014 adapter option for per-(agent, issue) workspace isolation across concurrent heartbeats ## Problem When a code-bound agent has `runtimeConfig.heartbeat.maxConcurrentRuns > 1` and Paperclip wakes the same agent on multiple issues in overlapping heartbeats, all spawned processes share the same on-disk working tree (the agent's adapter `cwd`). Concurrent `git checkout` / build / test runs on the same workspace race each other: - Heartbeat A checks out branch `feat/A`; heartbeat B checks out `feat/B`; A's `git status` now sees B's tree. - Untracked files written by one heartbeat show up to the other as \"someone else's in-progress work.\" - Worst case: a heartbeat commits or pus\n--- evidence id: issue:paperclipai/paperclip#6129:opened\n    opened 2026-05-16 by ddiall; 3 comments; labels: none\n    title: [Claude Local] Zero-credit ANTHROPIC_API_KEY blocks valid Claude Pro subscription and fails with opaque error\n    ### Description When setting up the `claude_local` adapter, if the host environment has an `ANTHROPIC_API_KEY` set (e.g., from other local dev workflows), Paperclip strictly forces the Claude CLI to use API-key billing. If that key has zero credits, the setup fails entirely, even if the user is actively logged into Claude Code with a valid, working Claude Pro subscription. This strict priority, combined with opaque error reporting, creates a frustrating \"false blocker\" for users who expect their subscription to just work. ### Problem 1: Strict API Key Priority Without Fallback Paperclip detects the environment variable and warns that it will override the subscription. However, if the API key\n--- evidence id: issue:paperclipai/paperclip#5986:opened\n    opened 2026-05-14 by aronprins; 3 comments; labels: none\n    title: plugin-llm-wiki npm install rewrites templates/.gitignore to .npmignore, breaking manifest import on 2026.513.0\n    ### Summary Installing `@paperclipai/plugin-llm-wiki` from npm on a clean Paperclip `2026.513.0` install fails during plugin install/bootstrap because the bundled manifest reads `templates/.gitignore`, but an npm install materializes that file as `templates/.npmignore`. This issue is a `2026.513.0` confirmation/addendum for #5818. The important correction is that the published tarball is **not** missing the template tree and is **not** missing `templates/.gitignore` at pack time; the failure happens after npm installs the package. ### Environment - Paperclip CLI package: `paperclipai@2026.513.0` - Plugin package: `@paperclipai/plugin-llm-wiki@0.1.0` (latest as of 2026-05-14) - Install type: \n--- evidence id: issue:paperclipai/paperclip#7047:opened\n    opened 2026-05-30 by mrveiss; 2 comments; labels: none\n    title: Default embedded postgres port 54329 conflicts with WSL2/Hyper-V dynamic port exclusion range\n    ## Bug Paperclip defaults `embeddedPostgresPort` to **54329** (hardcoded in `packages/shared/src/config-schema.ts:34` and `server/src/config.ts:305`). On Windows hosts running WSL2, Windows/Hyper-V dynamically reserves TCP port ranges. The range **54327\u201354426** is commonly reserved, causing all 20 ports in Paperclip's scan window (54329\u201354348) to return `EADDRINUSE`. ### Symptom Service crash-loops on every WSL2 restart: ``` Error: Embedded PostgreSQL could not find a free port from 54329 to 54348 at findAvailablePort (packages/db/src/migration-runtime.ts:75:9) ``` ### Root Cause Windows Hyper-V dynamic port exclusions (visible via `netsh interface ipv4 show excludedportrange protocol=tcp`) \n--- evidence id: issue:paperclipai/paperclip#6858:opened\n    opened 2026-05-28 by dmitry-cashtime; 2 comments; labels: none\n    title: Board API key TTL improvement\n    Hi team, We run Paperclip in production at CashTime Pay AG, where 13 AI agents (`AICEO`, `AIPM`, `AIBM-Recon`, `AIBM-Outreach`, `AICROPS`, `AISMM`, etc.) drive our daily Sales/Outreach pipeline. They depend on a Paperclip board API token (`pcp_board_*`) to: - Create heartbeat runs and wakeup requests via `POST /api/agents/:id/wakeup` - Create / comment / close Plane-mirrored issues - Coordinate handoffs between agents through `cashtime-agent-bus` This token sits in `~/stack/cashtime_agent_bus/.env` as `PAPERCLIP_API_TOKEN` and is read by a `systemd`-managed service that runs every 15 minutes (`cashtime-aibm-trigger.timer`). **Today our outreach pipeline silently broke** because the token exp\n--- evidence id: issue:paperclipai/paperclip#6800:opened\n    opened 2026-05-27 by brendandebeasi; 2 comments; labels: none\n    title: request_confirmation reject fires a comment-shaped wake with no comment \u2014 assignee resumes to `pending comments: 0/0` and reports no input; decline reason dropped\n    ## TL;DR Declining a `request_confirmation` wakes the assignee agent but hands it nothing to act on: no comment is created (the reject enqueues a wake tagged `issue_commented` with `sourceCommentId: null`), and the wake renderer has no `rejected` branch so it never reads the decline reason from `result.reason`. The agent resumes, sees `pending comments: 0/0`, reports \"no input,\" and re-parks the issue in `in_review`. The human's feedback is silently dropped. Same bug class as #3780 / #3781 (approvals), but in the interactions / `request_confirmation` path. ## Summary Declining a `request_confirmation` interaction (with `rejectRequiresReason: true`) wakes the assignee agent, but the wake carr\n--- evidence id: issue:paperclipai/paperclip#6798:opened\n    opened 2026-05-27 by more-than-ok-agents; 2 comments; labels: none\n    title: issue-level executionRunId lock with null checkoutRunId \u2014 no recovery path when run dies without releasing\n    ## Summary A companion issue to #6796, which covers `agents.status=error` with no diagnostic context. This issue covers the same root pattern at the **issue level**: `issues.executionRunId` can be left pointing at a now-dead run while `checkoutRunId` is `null`, leaving the issue in a state where every mutation route rejects with `Issue run ownership conflict` and **no self-serve recovery path exists**. --- ## Repro Observed in the wild on 2026-05-27 when a CEO agent attempted to close a done investigation issue. The run (`63e33617-82b0-4f15-a6b4-0352fb884ab0`) had already finished or died. **Issue state at time of observation:** - `executionRunId`: `63e33617-82b0-4f15-a6b4-0352fb884ab0` (sta\n--- evidence id: issue:paperclipai/paperclip#6717:opened\n    opened 2026-05-26 by more-than-ok-agents; 2 comments; labels: none\n    title: test\n    test\n--- evidence id: issue:paperclipai/paperclip#6672:opened\n    opened 2026-05-25 by gabrielhmsantos; 2 comments; labels: none\n    title: Daytona sandbox validation requires both image and snapshot, but rejects both when filled\n    When configuring a Daytona sandbox environment in Paperclip, the validation for `image` and `snapshot` appears to be inconsistent. The error message says: > Daytona sandbox environments must set either image or snapshot, not both. However, the current behavior makes it impossible to save the environment in some cases: 1. If I fill only `image`, the form returns: ```text Daytona snapshot cannot be empty. ``` <img width=\"1022\" height=\"68\" alt=\"Image\" src=\"https://github.com/user-attachments/assets/3c4eabe9-88dd-4cbf-8858-183bf90c9f94\" /> 2. If I fill only `snapshot`, the form returns: ```text Daytona image cannot be empty. ``` <img width=\"1030\" height=\"69\" alt=\"Image\" src=\"https://github.com/u\n--- evidence id: issue:paperclipai/paperclip#6552:opened\n    opened 2026-05-22 by astromaj23; 2 comments; labels: none\n    title: Bug: Gemini adapter fails hard on expired session resume instead of falling back to fresh session\n    ## Summary When using the `gemini_local` adapter, Paperclip persists session IDs in `agent_task_sessions` and attempts to resume them on subsequent heartbeat runs. Gemini sessions expire faster than Claude sessions (typically within 15-30 minutes of inactivity). When the resume fails with \"Invalid session identifier,\" Paperclip marks the entire run as `adapter_failed` instead of clearing the stale session and retrying with a fresh session. This causes agents to enter an error/idle flapping loop that wastes heartbeat cycles and produces zero useful work. ## Steps to Reproduce 1. Configure an agent with `adapter_type = gemini_local` 2. Let it complete a heartbeat run successfully (session ID s\n--- evidence id: issue:paperclipai/paperclip#6449:opened\n    opened 2026-05-20 by contentfree; 2 comments; labels: none\n    title: Comments on approvals should trigger response from agent requesting approval\n    Comments on approvals today appear to dead-end. Either commenting on approvals should be removed (with the expectation that the user will comment in the issue that generated the approval) or that comments on approvals cause some activity. I'd like to make a comment on an approval and then click \"Request Revision\"\n--- evidence id: issue:paperclipai/paperclip#6443:opened\n    opened 2026-05-20 by xxMIKSTERxx; 2 comments; labels: none\n    title: Hardening: refuse sensitive flags (`--api-key`, `--value`, `--secret`) via argv in non-TTY contexts\n    ## TL;DR `paperclipai` accepts sensitive material (`--api-key`, `--value` on `secrets create/update`, etc.) via argv with no guard against non-interactive contexts. When the CLI is invoked under a wrapper that captures or prints argv/stdout (pnpm's run banner, `docker run` without `-it`, CI loggers, systemd journal), the secret leaks into logs that the operator never intended to expose. Proposing a default-on guard: **refuse sensitive-flag argv values when `process.stdout.isTTY === false`**, with `--allow-sensitive-argv` (and `PAPERCLIP_ALLOW_SENSITIVE_ARGV=1`) as the explicit opt-in. Env-var resolution (`PAPERCLIP_API_KEY`, `--value-env`) is already the supported safe path \u2014 this just makes\n--- evidence id: issue:paperclipai/paperclip#6424:opened\n    opened 2026-05-20 by astromaj23; 2 comments; labels: none\n    title: [Bug] enableStrandedIssueRecovery: false does not prevent STRANDED ISSUE recovery cards from appearing\n    ## Summary Setting `enableStrandedIssueRecovery: false` in `instance_settings.experimental` does not prevent stranded issue recovery cards from being generated. The yellow \"RECOVERY NEEDED \u2014 STRANDED ISSUE\" cards continue to appear on tickets, requiring manual dismissal. ## Steps to Reproduce 1. Set the flag: ```sql UPDATE instance_settings SET experimental = experimental || '{\"enableStrandedIssueRecovery\": false}'::jsonb; ``` 2. Verify: `SELECT experimental->'enableStrandedIssueRecovery' FROM instance_settings;` returns `false` 3. Wait for an agent run to fail or get cancelled on a ticket 4. The ticket displays a yellow card: \"RECOVERY NEEDED \u2014 STRANDED ISSUE \u2014 Paperclip retried this issue'\n--- evidence id: issue:paperclipai/paperclip#6360:opened\n    opened 2026-05-19 by langenuts; 2 comments; labels: none\n    title: Bump hermes-paperclip-adapter to ^0.3.0 \u2014 current ^0.2.0 pin ships without env-resolution fix\n    ## Problem `@paperclipai/server` (currently `2026.513.0`) declares: \\`\\`\\`json \"hermes-paperclip-adapter\": \"^0.2.0\" \\`\\`\\` In npm semver, `^0.2.0` resolves to `>=0.2.0 <0.3.0`, so installs pin at `0.2.1`. But the relevant env-resolution fix is in `hermes-paperclip-adapter@0.3.0` \u2014 published, but unreachable from the current caret range. ## Impact `hermes_local` agents fail at runtime with: \\`\\`\\` Error: Error code: 401 - {'error': {'message': 'Invalid Authentication', 'type': 'invalid_authentication_error'}} \\`\\`\\` \u2026whenever \\`adapterConfig.env\\` contains envelope-format bindings (\\`{type: \"plain\", value: \"...\"}\\`), which is what the server actually persists. ### Root cause In `0.2.1`, `herm\n--- evidence id: issue:paperclipai/paperclip#6306:opened\n    opened 2026-05-18 by arnavbansal12; 2 comments; labels: none\n    title: Implement analytics tracking infrastructure (PostHog)\n    ## Objective Implement the analytics tracking infrastructure defined in `doc/ANALYTICS.md`. ## Deliverables 1. **PostHog Integration** \u2014 Install PostHog SDK in the UI, configure API keys 2. **Event Tracking** \u2014 Implement all events from the tracking schema (page views, engagement, conversion, retention) 3. **Dashboard Setup** \u2014 Create Executive Dashboard, Channel Performance Dashboard, Content Performance Dashboard 4. **Privacy Compliance** \u2014 IP anonymization, DNT respect, 12-month retention, no PII collection ## Reference - Analytics plan: `doc/ANALYTICS.md` - GTM Strategy: `doc/GTM-STRATEGY.md` \u00a77 ## Acceptance Criteria - [ ] All funnel stages tracked with events - [ ] Executive dashboard \n--- evidence id: issue:paperclipai/paperclip#6308:opened\n    opened 2026-05-18 by arnavbansal12; 2 comments; labels: none\n    title: Execute SEO strategy and publish first content pieces\n    ## Objective Execute the SEO strategy defined in `doc/GTM-STRATEGY.md` \u00a73.3 and Appendix B. ## Deliverables 1. **SEO Audit** \u2014 Current search presence, technical SEO issues 2. **Keyword Targeting** \u2014 Optimize README, docs, and landing page for target keywords 3. **Content Creation** \u2014 Write and publish \"What is an AI Company?\" and \"Paperclip vs LangGraph vs CrewAI\" 4. **Backlink Strategy** \u2014 Submit to developer communities, HN, Reddit, AI newsletters ## Reference - GTM Strategy: `doc/GTM-STRATEGY.md` \u00a73.3, Appendix B - Launch Content: `doc/LAUNCH-CONTENT.md` ## Acceptance Criteria - [ ] Top 20 target keywords identified and documented - [ ] README and docs optimized for search - [ ] First 2 \n--- evidence id: issue:paperclipai/paperclip#6217:opened\n    opened 2026-05-17 by allanyim-liinnks; 2 comments; labels: none\n    title: Hourly backup cadence creates ~28GB permanent baseline \u2014 no UI/API to reduce frequency\n    # Hourly backup cadence creates ~28GB permanent baseline \u2014 no discovered UI/API to reduce frequency, fills small disks ## Summary Paperclip's automatic database backup runs every 60 minutes (per `intervalMinutes:60` in `Automatic database backups enabled` log). With the default `backupRetention.dailyDays:7`, this creates a permanent **~28GB baseline** (168 hourly files \u00d7 ~169MB compressed dump) that the retention policy correctly caps but cannot reduce. On a disk already pressured by other consumers, this baseline contributes to free-space exhaustion that eventually breaks the embedded PostgreSQL (temp file creation fails). Retention itself appears to work (oldest file in our directory was ~\n--- evidence id: issue:paperclipai/paperclip#6201:opened\n    opened 2026-05-17 by leebaroneau; 2 comments; labels: none\n    title: Add Hermes runtime identity hook for stable profiles\n    ## Goal Add a stable Paperclip-managed runtime identity for every `hermes_local` agent so a newly created Paperclip agent maps to one durable Hermes profile. ## Why Paperclip needs a stable adapter-level identity before tool policy can be rendered safely. Without this, the tool dashboard work has nowhere durable to write per-agent Hermes configuration. ## Scope - Extend the server adapter contract with an optional idempotent `ensureRuntimeIdentity` hook. - Implement the hook for `hermes_local`. - Create a Hermes profile directory under the Paperclip instance root, for example `runtimes/hermes/profiles/<slug>`. - Patch agent `adapterConfig.env.HERMES_HOME` and `metadata.runtimeIdentity`. - Ca\n--- evidence id: issue:paperclipai/paperclip#6189:opened\n    opened 2026-05-17 by jodioyang-design; 2 comments; labels: none\n    title: Bug: readLocalSkillImports drops sibling files when SKILL.md is at the resolved root (fileInventory empty, trust downgrade)\n    ## Bug `readLocalSkillImports` drops every sibling of `SKILL.md` from `fileInventory` when the importer is pointed at a skill directory whose `SKILL.md` sits at the resolved root. Source: `server/src/services/company-skills.ts`, function `readLocalSkillImports` (around line 1029). ### Why ```ts const skillDir = path.posix.dirname(skillPath); const inventory = allFiles .filter((entry) => entry === skillPath || entry.startsWith(`${skillDir}/`)) .map((entry) => { const relative = entry === skillPath ? \"SKILL.md\" : entry.slice(skillDir.length + 1); \u2026 }); ``` `walkLocalFiles` produces paths relative to the resolved root, so when `SKILL.md` is at the root the walker emits `SKILL.md`, `references/f\n--- evidence id: issue:paperclipai/paperclip#6146:opened\n    opened 2026-05-16 by euhenriquecosta; 2 comments; labels: none\n    title: Bug: docker-entrypoint.sh skips chown on fresh volume, causing EACCES crash\n    ## Bug Report ### Summary On a **fresh deployment** (empty volume), the Paperclip container crashes immediately with `EACCES: permission denied, open '/paperclip/instances/default/.env'`. The `docker-entrypoint.sh` only runs `chown -R node:node /paperclip` when a UID/GID remap is detected, but on a default install (UID 1000, which already matches the `node` user), the chown is **skipped entirely**. Files created during first startup are owned by `root:root` with `0600` permissions, and the app \u2014 which drops to the `node` user via `gosu` \u2014 cannot read them. ### Environment - **Image:** `ghcr.io/paperclipai/paperclip:latest` (`sha256:f61a636070...`) - **Runtime:** Docker Swarm via Portainer - \n--- evidence id: issue:paperclipai/paperclip#6128:opened\n    opened 2026-05-16 by belousov-petr; 2 comments; labels: none\n    title: Agents with secret_ref env entries break after v2026.512.0 \u2014 migration 0082_dry_vision doesn't backfill company_secret_bindings\n    ## Summary After upgrading from `v2026.428.0` to `v2026.513.0`, every agent on my install that resolves API keys through `adapterConfig.env[*].type === \"secret_ref\"` started failing at heartbeat with: ``` Secret is not bound to agent:<agent_id> at env.<ENV_VAR_NAME> ``` 17 of 18 agents on the affected instance went dark on restart. The one that didn't was the only agent with no `secret_ref` entries. ## Reproduction Pre-upgrade state (worked on `v2026.428.0`): ```json // agents.adapterConfig { \"env\": { \"MY_PROVIDER_API_KEY\": { \"type\": \"secret_ref\", \"version\": \"latest\", \"secretId\": \"<company-secret-uuid>\" } } } ``` Steps: 1. Run on `v2026.428.0` with at least one agent that has `secret_ref` en\n--- evidence id: issue:paperclipai/paperclip#6057:opened\n    opened 2026-05-15 by tiangao88; 2 comments; labels: none\n    title: Re-enable plugin secret refs with company-scoped config and runtime secret resolution\n    ## Problem `PAP-2394` correctly fails closed by rejecting plugin config that contains plugin secret refs and disabling plugin worker secret resolution. That prevents the previous cross-company exposure path from remaining available, but it also means plugins cannot currently use secret refs safely. The missing boundary is company scope carried end to end: - plugin config storage is instance-global instead of company-scoped - secret bindings are company-scoped - plugin runtime calls do not consistently carry an active company context into config reads and secret resolution This makes it unsafe to re-enable plugin secret refs until config access and runtime secret resolution are both mediated \n--- evidence id: issue:paperclipai/paperclip#5985:opened\n    opened 2026-05-14 by Elhadnaits; 2 comments; labels: none\n    title: Feature/bug: issue_blockers_resolved wake not implemented \u2014 blocked issues stay stuck after blockers complete\n    ## Summary The `issue_blockers_resolved` wake reason is documented in the Paperclip agent skill but is not implemented in the server. When all `blockedBy` issues on a ticket reach `done`, no wake fires and the blocked issue remains `blocked` indefinitely. ## Environment - **paperclipai version:** 2026.513.0 - **Reproduced:** 2026-05-14 ## Expected behavior (per skill docs) > **Automatic wakes:** > - `PAPERCLIP_WAKE_REASON=issue_blockers_resolved` \u2014 all `blockedBy` issues reached `done`; dependent's assignee is woken. Two events should occur: 1. A wake fires to the blocked issue's assignee with `PAPERCLIP_WAKE_REASON=issue_blockers_resolved` 2. The blocked issue auto-transitions from `blocked\n--- evidence id: issue:paperclipai/paperclip#7257:opened\n    opened 2026-05-31 by xcellencecustomer-code; 1 comments; labels: none\n    title: Feature Request: API endpoint to assign skills to agents programmatically\n    ## Context I'm running Paperclip for my company and I have custom skills already imported (via GitHub repo). However, I cannot programmatically assign these skills to specific agents via the API. ## Current State - Skills are successfully imported into the company workspace (GET /api/companies/{id}/skills works) - Agents are configured (GET /api/companies/{id}/agents works) - There is no API endpoint to assign skills to agents ## Request Add an API endpoint: ``` PUT /api/agents/{id}/skills Body: {\"skills\": [\"skill-id-1\", \"skill-id-2\"]} ``` Or alternatively, allow the CEO agent to auto-assign skills to agents based on their role/name. ## Use Case I have 11 agents and 9 custom Amazon FBA skill\n--- evidence id: issue:paperclipai/paperclip#7255:opened\n    opened 2026-05-31 by janzhou123; 1 comments; labels: none\n    title: pnpm install bug here\n    pnpm i Scope: all 28 workspace projects Lockfile is up to date, resolution step is skipped Packages: +1229 +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ Progress: resolved 1229, reused 1224, downloaded 0, added 1229, done WARN Failed to create bin at /Users/aa/Documents/tools/paperclip-2026.529.0/packages/plugins/create-paperclip-plugin/node_modules/.bin/paperclip-plugin-dev-server. ENOENT: no such file or directory, open '/Users/aa/Documents/tools/paperclip-2026.529.0/packages/plugins/sdk/dist/dev-cli.js' WARN Failed to create bin at /Users/aa/Documents/tools/paperclip-2026.529.0/\n--- evidence id: issue:paperclipai/paperclip#7245:opened\n    opened 2026-05-30 by CosmicDreamer17; 1 comments; labels: none\n    title: company import --target new strands imported agents in pending_approval with no approval record (no UI button, no CLI verb -> unactivatable)\n    # `company import --target new` strands imported agents in `pending_approval` with no approval record (no UI button, no CLI verb \u2192 unactivatable) ## Summary On a self-hosted instance, importing a company with `company import --target new` into a target company that has `requireBoardApprovalForNewAgents: true` leaves every imported agent in status `pending_approval` **but never mints the corresponding `hire_agent` approval record**. The result is a dead state with no supported recovery path: - `GET /api/companies/{id}/approvals` (CLI `approval list -C {id}`) returns **empty** \u2014 there is nothing to approve. - The web UI exposes no \"approve\" action for the agent (only *Reset session* and *Termi", "response": {"ranked": [{"evidence_id": "issue:paperclipai/paperclip#6653", "first_step": "Open the UI file RequestConfirmationCard, add the missing MarkdownBody rendering for payload.prompt in the pending-state branch (mirror the existing payload.detailsMarkdown handling), run the UI dev build and verify request_confirmation prompts render Markdown.", "why": "Reporter pinpoints a one-line omission in RequestConfirmationCard (payload.detailsMarkdown already uses MarkdownBody while payload.prompt does not), giving a precise, small front-end change with repro steps and high chance of merge (clear bug and bounded fix)."}, {"evidence_id": "issue:paperclipai/paperclip#6189", "first_step": "Edit server/src/services/company-skills.ts readLocalSkillImports to correctly compute the skillDir when SKILL.md is at the resolved root (handle '.'/root case or use path.relative) and add a unit/integration test reproducing the dropped siblings.", "why": "The bug is localized with quoted source lines showing the exact filter logic that drops siblings when SKILL.md is at the repo root, so an outsider can implement the directory-root fix and add tests without private context."}, {"evidence_id": "issue:paperclipai/paperclip#7281", "first_step": "Modify the skills-catalog manifest lookup in server services/skills-catalog (the getCatalogManifest callsite) to resolve the manifest from the installed package layout (fall back from monorepo path to package-relative generated/catalog.json), add a unit test or repro script, and verify GET /api/skills/catalog on a fresh global install.", "why": "Stack trace and error message show the server is resolving a monorepo path that doesn't exist in published npm installs, a concrete path-resolution fix with clear repro that an external contributor can implement and test."}, {"evidence_id": "issue:paperclipai/paperclip#6146", "first_step": "Update docker-entrypoint.sh to always chown the instance directory on first-run (not only when UID/GID remap detected), add a safe conditional for idempotence, rebuild the image locally and verify container no longer crashes on fresh volume.", "why": "Failure is reproducible and the root cause is a simple entrypoint script omission (chown skipped), so a small change to the script with a regression test in Docker will likely be accepted."}, {"evidence_id": "issue:paperclipai/paperclip#6606", "first_step": "In the OpenClaw gateway adapter code, remove or conditionally omit the top-level 'paperclip' property from agent params when targeting protocol-4 gateways (or make the adapter emit protocol-4 compatible shape), add adapter tests against protocol-4 schema, and validate agents can connect to OpenClaw v2026.5.20+.", "why": "Reporter shows a concrete protocol-schema rejection and includes diffs between stable and hotpatch; the fix is a localized change to adapter serialization to make it compatible with protocol 4 and is narrowly scoped and testable."}]}, "usage": {"input_tokens": 9134, "output_tokens": 1517}}
